"""GeoSWMM Gateway — FastAPI proxy for OpenRouter with a local pgvector store.

The package layout follows §10 of ``GatewayServerPlan.md``. See ``CLAUDE.md``
for project conventions (async everywhere, no buffered streams, secrets only
in ``.env``, etc.).
"""

__all__: list[str] = []
